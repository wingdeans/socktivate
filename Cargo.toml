[package]
name = "socktivate"
edition = "2024"

[dependencies]
anyhow = "1.0.100"
libc = "0.2.177"
rustix = { version = "1.1.2", features = ["event", "net", "process", "thread"] }
# nix = { version = "0.30.1", features = ["net", "socket"] }

